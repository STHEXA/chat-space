.side-container
  - current_user.groups.each do |group|
    = link_to '#' do
      .side-container__group--first
        %p.side-container__name 
          = group.name
        %p.side-container__message
          まだメッセージがありません
      